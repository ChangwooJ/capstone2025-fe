export interface SignUpFormType {
    email: string;
    password: string;
    passwordCheck: string;
    username: string;
}

export interface LoginFormType {
    email: string;
    password: string;
}